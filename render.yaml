services:
  - type: web
    name: gtasks-mcp
    env: node
    branch: main
    buildCommand: npm install && npm run build
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
